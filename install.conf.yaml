- defaults:
    link:
      relink: true
      force: true
- clean: ["~"]
- link:
    ~/.zshrc: zshrc
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.tmux: tmux
    ~/antigen.zsh: antigen.zsh

- shell:
    - [git submodule update --init --recursive, Installing submodules]
